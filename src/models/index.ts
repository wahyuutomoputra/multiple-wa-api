import DATABASE from "../lib/system/database";
import Messages from "./messages.model";
import Devices from "./devices.model";

const model = {
    DATABASE,
    Messages,
    Devices
}

export default model;